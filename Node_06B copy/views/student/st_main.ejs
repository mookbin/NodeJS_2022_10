<!DOCTYPE html>
<html lang="ko">
  <head>
    <%-include ("../includes/head.ejs") %>
    <link ref="stylesheet" />
    <style>
      /* main.css 에서 이미 section.main 을 설정하고 있지만
      student 정보를 표현하는 화면에서
       width 와 margin 부분을 다시 재정의
       */
      section.main {
        width: 95%;
        margin: 0px auto;
      }
      .student.search {
        display: flex;
        justify-content: center;
      }

      .student.insert {
        text-align: right;
        padding: 0.8em;
      }

      .student.insert a {
        flex: 1;
        padding: 6px 10px;
        margin: 5px;
        background-color: green;
        color: white;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <%- include ("../includes/header.ejs") %> <% if(body == "list") { %>
    <article class="student search">
      <form>
        <input name="st_name" placeholder="학생이름을 입력 후 Enter" />
      </form>
    </article>
    <%}%>
    <section class="main">
      <article>
        <% if(body =="list"){ %> <%- include ("./st_list.ejs") %> <%}%> <%
        if(body =="write"){ %> <%- include ("./st_write.ejs") %> <%}%>
      </article>
    </section>
    <% if(body == "list") { %>
    <article class="student insert">
      <a href="/student/insert">학생정보추가</a>
    </article>
    <%}%> <%- include ("../includes/footer.ejs") %>
  </body>
</html>
